<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Page of {{player.name}}</title>
    <link rel="stylesheet" href="../styles/teamstyles.css">
</head>
<body>
    <a href="/">Home</a>
    <a href="/players">Back to players</a>

        {{#if player}}

        <h2 style="color:white">Page of {{player.name}}</h2>
        <h3>{{player.name}}</h3>
          <h3>Number:{{player.number}}</h3>
          <h4>Team:{{player.team}}</h4>
          <h5 style="color:white">All Star Game {{player.isAllStar}}</h5>

            <a href="/players/{{player._id}}/edit/"></a>
          <button>Edit Player</button>

          {{else}}
          <h3>Player does not exists</h3>
          {{/if}}
</body>
</html>